# game/config.py — ตัวเลขฝั่ง server ทั้งหมดอยู่ที่นี่ที่เดียว
#
# คู่กับ src/config.js ของฝั่ง client (คนละไฟล์เพราะคนละภาษา คนละหน้าที่)
#   server = กติกา (เกิดที่ไหน เดินยังไง ใครยิงโดน)   ← ไฟล์นี้
#   client = ความรู้สึก (สั่น แฟลช เสียง ขนาดตัวอักษร)
#
# ค่าที่ "ทั้งสองฝั่งต้องตรงกัน" (เช่น bulletTravelMs) ถือว่า server เป็นเจ้าของ
# แล้วส่งไปให้ client ตอน hello — ห้ามก๊อปตัวเลขไปไว้สองที่

import os

# ═══ Network ═══════════════════════════════════════════════
HOST = "0.0.0.0"
# hoster (Render/Railway/Fly/Heroku) ส่งพอร์ตมาทาง $PORT — ต้องฟังตัวนั้นก่อน ไม่งั้นโดนมองว่าแอปตาย
PORT = int(os.environ.get("PORT") or os.environ.get("SPACEHT_PORT") or 5000)

# URL ที่จะยัดเข้า QR — ปกติเว้นว่างให้ detect IP เอง
# ถ้าใช้ cloudflared: set SPACEHT_PUBLIC_URL=https://xxx.trycloudflare.com
PUBLIC_URL = os.environ.get("SPACEHT_PUBLIC_URL", "").rstrip("/")

# Windows Mobile Hotspot ให้ IP นี้เสมอ → QR fix ได้ ไม่ต้องมีเน็ต
HOTSPOT_IP = "192.168.137.1"

# ═══ รอบเกม (Phase C) ══════════════════════════════════════
# ── โครงรอบ (F4) ──
# 0:00-0:50 ปกติ ยิงนัดเดียวแตก · 0:50-1:15 พายุ ยิง 2-5 นัด · 1:15 จบทันที
ROUND_SEC       = 75
STORM_START_SEC = 50
STORM_END_SEC   = 75
STORM_PASS_RATE = 0.60      # สอยได้ถึงเกณฑ์นี้ = ผ่าน
STORM_PASS_MULT = 2.0
STORM_FAIL_MULT = 0.5       # ห้ามใช้ 0 — เด็กโดนล้างคะแนนจะเดินออกจากบูธ

# พายุดึงเฉพาะดวงที่ไหม้เร็ว (ยังเป็นข้อมูลจริง 100% แค่คัดกลุ่ม)
STORM_MAX_DURATION = 0.35
# ดวง/คน ในช่วงศึกเดือด — 1 คน 20 ดวง · 5 คน 100 ดวง
# ตั้งเป็นจำนวนตรงๆ ไม่ใช่ตัวคูณ จะได้อ่านออกได้เลยว่าหน้างานจะเจอกี่ดวง
STORM_METEORS_PER_PLAYER = 20

# จำนวนนัดที่ต้องยิงในช่วงพายุ มาจาก mag จริง — ก้อนใหญ่ = สว่าง = ทนกว่า
# (สอนฟิสิกส์ฟรีโดยไม่ต้องอธิบาย)
STORM_HP_BY_MAG = [(-2.0, 5), (-0.5, 4), (1.0, 3), (99.0, 2)]
STORM_MIN_SCALE = 0.60      # ก้อนหดตามความเสียหาย แต่ห้ามต่ำกว่านี้
                            # ไม่งั้นนัดท้ายๆ ยิงยากขึ้น = ลงโทษคนที่ยิงถูก

# ═══ QTE ปิดท้ายรอบ (F10) ══════════════════════════════════
# หมดเวลา 1:15 แล้วยังไม่จบทันที — มี "ลูกไฟดวงสุดท้าย" ให้ทั้งทีมรุมยิงพร้อมกัน
# เป็นดวงจริงจาก GMN เหมือนทุกดวง แค่คัดเอาดวงที่ mag สว่างที่สุด (fireball ของจริง)
# และมันก็ยังไหม้หมดกลางอากาศเหมือนเดิม — ไม่มีดวงไหนถึงพื้น
QTE_SEC = 12                 # หน้าต่าง QTE ยาวเท่านี้ แล้วจบรอบ
QTE_TAPS_PER_PLAYER = 30     # ต้องรัวให้ครบเท่านี้ต่อคน ทีมใหญ่ก็ต้องรัวมากขึ้นตามกัน
QTE_BONUS = 4000             # โบนัสเต็มเมื่อครบ (บวก *หลัง* คูณพายุ ไม่โดนพายุหาร)
QTE_MIN_TAP_MS = 90          # กันสคริปต์/กดค้างรัวเกินมนุษย์ — ต่อผู้เล่น 1 คน
QTE_MAG_MAX = -3.0           # คัดเฉพาะดวงที่สว่างระดับ fireball
QTE_YAW_LIMIT_DEG = 48       # ดวงสุดท้ายต้องอยู่กลางซุ้ม ทุกคนจะได้เห็นพร้อมกัน
QTE_SIZE_MULT = 2.2          # โตกว่าปกติ — ดวงเดียวที่ทั้งทีมรุมยิง ต้องเห็นชัด

COUNTDOWN_SEC = 3
# จบรอบแล้วกี่วินาทีถึงกลับ lobby เอง — บูธจะได้ต่อคิวรอบถัดไปโดยไม่ต้องกดอะไร
RESET_AFTER_SEC = 25
ROOM_TTL_SEC = 900           # ห้องร้างเกินนี้เก็บทิ้ง
SCHEDULE_MARGIN = 1.35       # จัดตารางเผื่อไว้เกินจำนวนที่คาดว่าจะใช้
SPAWN_JITTER = 0.7           # เวลาเกิดเหวี่ยงได้กี่ส่วนของช่องไฟ

# ทุกทีมในความยากเดียวกันเจอชุดเดียวกันเป๊ะ → leaderboard เทียบกันได้จริง (spec §2 แนะนำให้เปิด)
FIXED_SEED_PER_DIFFICULTY = True
FIXED_SEED_BASE = 20260812

TEAM_NAME_MIN = 3
TEAM_NAME_MAX = 12

# hardcode ตามที่สั่ง — ไม่อ่าน env แล้ว บูธจะได้ไม่มีทางเข้าไม่ได้เพราะลืม set
ADMIN_PASSWORD = "ilovefocalors1437"
# ชื่อทีมของห้อง admin — คะแนนลงบอร์ด ADMIN ไม่ปนกับทีมเด็ก
ADMIN_TEAM_NAME = "ADMIN"

# ═══ Room / slot ═══════════════════════════════════════════
# กี่คนก็เล่นได้ — เข้าได้เรื่อยๆ จนกว่า admin จะกดเริ่ม
# ตั้งเพดานไว้กันอุบัติเหตุ (เด็กทั้งห้องแห่เข้าห้องเดียว) ไม่ใช่กันโดยตั้งใจ
MAX_SLOTS = 5           # ทีมละ 1-5 คน

# สีประจำผู้เล่น — ต้องแยกออกจากกันชัดๆ ตอนดูบนจอ admin
# ห้ามใช้สีที่ชนกับสีอุกกาบาต (ส้ม) หรือสี telegraph (แดงเข้ม)
SLOT_COLORS = [
    {"name": "แดง",     "en": "RED",    "hex": "#ff4d6d", "rgb": [1.00, 0.30, 0.43]},
    {"name": "ฟ้า",     "en": "BLUE",   "hex": "#59c0ff", "rgb": [0.35, 0.75, 1.00]},
    {"name": "เขียว",    "en": "GREEN",  "hex": "#8fffa8", "rgb": [0.56, 1.00, 0.66]},
    {"name": "ม่วง",     "en": "PURPLE", "hex": "#c58bff", "rgb": [0.77, 0.55, 1.00]},
    {"name": "เหลือง",   "en": "YELLOW", "hex": "#ffe066", "rgb": [1.00, 0.88, 0.40]},
    {"name": "ชมพู",     "en": "PINK",   "hex": "#ff9ed8", "rgb": [1.00, 0.62, 0.85]},
    {"name": "ฟ้าอมเขียว", "en": "TEAL",   "hex": "#5fe4d0", "rgb": [0.37, 0.89, 0.82]},
    {"name": "ส้มอ่อน",  "en": "PEACH",  "hex": "#ffb37a", "rgb": [1.00, 0.70, 0.48]},
    {"name": "คราม",     "en": "INDIGO", "hex": "#8f9bff", "rgb": [0.56, 0.61, 1.00]},
    {"name": "มะนาว",    "en": "LIME",   "hex": "#c8ff6b", "rgb": [0.78, 1.00, 0.42]},
    {"name": "ฟ้าน้ำแข็ง", "en": "ICE",    "hex": "#a8e8ff", "rgb": [0.66, 0.91, 1.00]},
    {"name": "แสด",      "en": "AMBER",  "hex": "#ffcf4d", "rgb": [1.00, 0.81, 0.30]},
]

# admin เท่านั้นที่กดเริ่มรอบได้ (ผู้เล่นเข้าได้เรื่อยๆ จนกว่าจะกด)
# ตั้ง True ถ้าอยากให้ผู้เล่นกดเริ่มเองได้ด้วย (สะดวกตอนเทสต์คนเดียว)
ALLOW_PLAYER_START = False

# มือถือหลุด (ล็อกจอ/refresh/กด Home) แล้วกลับมาภายในกี่วิ ยังได้ slot เดิม
# นานกว่านี้ถึงจะปล่อยให้คนใหม่มาแทน — เด็ก refresh แล้วต้องไม่กลายเป็น player 4
SLOT_RECLAIM_SEC = 90

# ═══ เวลา ═══════════════════════════════════════════════════
TICK_HZ = 30          # server loop
AIM_BROADCAST_HZ = 30 # ส่ง aim ของทุกคนไปจอใหญ่ (เล็กมาก แค่ 2 float ต่อคน)
STATE_HZ = 5          # คะแนน/combo — spec บอก throttle ~5Hz

# ═══ กระสุน (server เป็นเจ้าของ client ยืมไปใช้วาด) ═════════
BULLET_TRAVEL_MS = 150     # ตรงกับ Phase A — latency LAN ซ่อนอยู่ในนี้พอดี
FIRE_COOLDOWN_MS = 190

# ═══ Aim assist (ตัดสินที่ server เพราะ hit ตัดสินที่ server) ═
ASSIST_LOCKON_DEG = 4.0
ASSIST_CORRECTION = 0.95
ASSIST_FALLOFF = 1.4
HIT_RADIUS_BONUS = 1.3

# ═══ อุกกาบาต — กติกาการเกิดและเส้นทาง ═════════════════════
YAW_LIMIT_DEG = 90         # ซุ้ม ±90° ห้ามเปลี่ยน
YAW_MARGIN_DEG = 8
YAW_DRIFT_DEG = 32

GRAVITY = -7.5
SPAWN_DIST = 600.0
SPAWN_ALT_MIN = 280.0
SPAWN_ALT_MAX = 480.0
IMPACT_DIST_MIN = 55.0
IMPACT_DIST_MAX = 210.0
FLIGHT_SEC_MIN = 5.0
FLIGHT_SEC_MAX = 8.2
SIZE_MIN = 3.0
SIZE_MAX = 5.5
SPIN_SPEED = 1.6

# telegraph = ช่วงที่ "ยังไม่เข้าบรรยากาศ" — ยิงได้แล้ว (spec B §7)
# ต่างจาก Phase A ที่ห้ามยิงตอน telegraph
TELEGRAPH_MS = 1500

MAX_ALIVE = 18

# ═══ Spawner ═══════════════════════════════════════════════
# ดวง/นาที *ต่อผู้เล่น 1 คน* — 1 คนเจอ 10 ดวง/นาที, 3 คนเจอ 30
# ทีมใหญ่ไม่ได้เปรียบเพราะงานต่อหัวเท่ากัน (อุกกาบาตกองเดียวกัน)
SPAWN_RATE_PER_MIN = 10.0
FIRST_SPAWN_DELAY_MS = 2500

# ═══ คะแนน ═════════════════════════════════════════════════
SCORE_PER_HIT = 100
COMBO_MAX = 8
COMBO_WINDOW_MS = 3500
# ปล่อยให้ไหม้หมด = เสียคะแนน + combo ขาด (ไม่ใช่ HP เมือง — นั่นคือ Phase C)
SCORE_PENALTY_BURNUP = 50


# ═══ GMN (เปิดใช้ตอน B7) ═══════════════════════════════════
DB_PATH = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
                       "data", "meteors.db")
USE_GMN = True
OBSERVER_LAT = 13.7563      # กรุงเทพ
OBSERVER_LON = 100.5018
CONTACT_LOG_KEEP = 8

# แมพข้อมูลจริง → พารามิเตอร์ในเกม (ต้องตรงกับ src/config.js ฝั่ง client)
GMN_SPAWN_RANGE = 620.0     # ระยะจากผู้เล่นถึงจุดเกิด วัดตามแนวเรดิแอนต์
GMN_VGEO_SLOW = 15.0        # km/s ช้ากว่านี้ = บินนานสุด
GMN_VGEO_FAST = 70.0
GMN_MAG_BRIGHT = -6.0       # สว่างกว่านี้ = ใหญ่สุด
GMN_MAG_DIM = 3.0

# ── สถิติจริงไว้โชว์ตอนจบรอบ (§5.3) ──
GMN_CAMERAS_WORLDWIDE = 1000
GMN_COUNTRIES = 40
